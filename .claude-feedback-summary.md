## Email Draft Fix: Use Logged-In User Details

**Issue**: Email drafts on the Outreach page were hardcoded to use Wesley's information instead of the logged-in user's details.

**Changes Made**:
- Modified `draftEmail` mutation in `server/routers.ts:2848` to access user context via `ctx` parameter
- Replaced hardcoded "Wesley Roos" with dynamic `ctx.user?.name || 'the team'`  
- Updated email template to use logged-in user's name in both the system prompt and email signature
- Removed hardcoded phone number since user profile doesn't store phone numbers

**Files Touched**: `server/routers.ts` (lines 2848-2886)

**Result**: Email drafts now populate with the logged-in user's name and information instead of always using Wesley's details.