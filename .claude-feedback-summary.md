# Fix: duplicate invoices on failed mandate charges + bulk invoice delete

- `server/scheduler.ts` — `runMandateBillingTick` now reuses an existing unpaid mandate invoice instead of creating a new one per retry, uses a unique Paystack reference per attempt, and on a synchronous decline marks the invoice overdue, marks the mandate failed (stopping hourly retries), and emails the owner.
- `server/db.ts` — added `getUnpaidMandateInvoice(mandateId)`.
- `server/routers.ts` — added `invoice.bulkDelete` (admin, array of invoice numbers).
- `client/src/pages/ClientPortal.tsx` — billing table now has a checkbox column with select-all, a selection toolbar, and a confirm dialog for bulk delete.
- `pnpm build` passes; the 8 failing tests in `server/invoice.test.ts` are pre-existing (fail on clean main too).
